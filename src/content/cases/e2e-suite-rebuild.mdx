---
title: "Shift Left at Scale: From 20% to 95% Automation Coverage"
titleFr: "Shift Left à Grande Échelle : De 20% à 95% de Couverture Automatisée"
client: "Retail French Leader"
year: 2026
category: "E2E Suite Rebuild"
stack: ["Espresso", "Mobile", "Kotlin", "API"]
metrics:
  - value: "95%"
    label: "Automation coverage"
    labelFr: "Couverture automatisée"
  - value: ">80%"
    label: "DDE"
    labelFr: "DDE"
  - value: ">80%"
    label: "DRE"
    labelFr: "DRE"
excerpt: "A large retail product team with low automated test coverage and a quality process still too dependent on late-stage validation. Built and expanded the mobile automation strategy with Espresso and Kotlin, supported developers in growing their testing skills, introduced shift-left practices, and explored AI proof of concepts to improve quality earlier in the delivery cycle."
excerptFr: "Une grande équipe produit retail avec une faible couverture de tests automatisés et un processus qualité encore trop dépendant de la validation tardive. Construction et expansion de la stratégie d'automatisation mobile avec Espresso et Kotlin, accompagnement des développeurs dans le développement de leurs compétences en test, introduction des pratiques shift-left et exploration de preuves de concept IA pour améliorer la qualité plus tôt dans le cycle de livraison."
---

## The problem

The team had stopped trusting their test suite. 35% of tests failed on any given CI run — not because of bugs, but because of race conditions, shared state, and brittle selectors. The response was to add retries. Three retries per test. The suite took 40 minutes.

When the CI dashboard shows red so often that green becomes surprising, the suite has failed its job.

## The approach

**Phase 1: Audit.** Categorised every failing test: race condition, shared state, broken selector, or legitimate bug. 80% of failures were automation problems, not product bugs.

**Phase 2: Redesign.** Replaced the flat test structure with a proper POM. Created shared fixtures for auth, test data creation, and cleanup. Rewrote selectors to use `getByRole`, `getByLabel`, and `data-testid` attributes.

**Phase 3: Rebuild.** Migrated incrementally — Playwright running in parallel with Cypress, both in CI, until the new suite matched coverage.

**Phase 4: Handoff.** Runbook, live walkthrough, first maintenance PR written by the client team.

## The outcome

Zero flake rate on 450 tests across 6 weeks of production CI runs. Run time dropped from 40 minutes to 13 minutes through parallelisation. The team's first response to a CI failure is now "what broke?" — not "probably flaky, retry it."
